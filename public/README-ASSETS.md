Add these files to this `public/` folder:

- `resume.pdf` — your resume, linked from the nav, the "30 seconds?" panel, and the recruiter view drawer.

Optional:
- `og-image.png` (1200x630) if you want a custom social preview image — then add it to the `openGraph`/`twitter` metadata in `app/layout.tsx`.
